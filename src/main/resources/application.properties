# Permitir conexões de qualquer IP (para desenvolvimento)
server.address=0.0.0.0

# Porta (use uma fixa para facilitar)
server.port=8080

# Se precisar de HTTPS
# server.ssl.enabled=true
# server.port=8443